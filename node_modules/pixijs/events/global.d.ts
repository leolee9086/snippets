declare namespace GlobalMixins
{
    type FederatedEventEmitterTypes = import('pixijs/events').FederatedEventEmitterTypes;
    type FederatedEventTarget = import('pixijs/events').FederatedEventTarget;

    // eslint-disable-next-line @typescript-eslint/no-empty-interface
    interface DisplayObject extends FederatedEventTarget
    {

    }

    // eslint-disable-next-line @typescript-eslint/no-empty-interface
    interface DisplayObjectEvents extends FederatedEventEmitterTypes
    {

    }

    interface IRenderer
    {
        readonly events: import('pixijs/events').EventSystem;
    }

    interface Renderer
    {
        readonly events: import('pixijs/events').EventSystem;
    }

    interface CanvasRenderer
    {
        readonly events: import('pixijs/events').EventSystem;
    }
}
