declare namespace GlobalMixins
{
    interface Graphics
    {
        _renderCanvas(renderer: import('pixijs/renderer/canvas').CanvasRenderer): void;
        generateCanvasTexture(scaleMode?: import('pixijs/constants').SCALE_MODES, resolution?: number): Texture;
        cachedGraphicsData: import('pixijs/graphics').GraphicsData[];
    }
}
