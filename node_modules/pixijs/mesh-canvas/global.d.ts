declare namespace GlobalMixins
{
    interface Mesh
    {
        _renderCanvas(renderer: import('pixijs/renderer/canvas').CanvasRenderer): void;
        _canvasPadding: number;
        canvasPadding: number;
        _cachedTint: number;
        _tintedCanvas: import('pixijs/settings').ICanvas | HTMLImageElement;
        _cachedTexture: import('pixijs/core').Texture;
    }

    interface MeshMaterial
    {
        _renderCanvas(renderer: import('pixijs/renderer/canvas').CanvasRenderer, mesh: import('pixijs/mesh').Mesh): void;
    }

    interface NineSlicePlane
    {
        _cachedTint: number;
        _tintedCanvas: import('pixijs/settings').ICanvas | HTMLImageElement;
        _canvasUvs: number[];
    }
}
