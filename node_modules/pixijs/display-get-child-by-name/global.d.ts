declare namespace GlobalMixins
{
    interface DisplayObject
    {
        name: string;
    }

    interface Container
    {
        getChildByName<T extends import('pixijs/display').DisplayObject = import('pixijs/display').DisplayObject>(
            name: string,
            deep?: boolean,
        ): T;
    }
}
