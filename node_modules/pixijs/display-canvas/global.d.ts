declare namespace GlobalMixins
{
    interface DisplayObject
    {
        renderCanvas?(renderer: import('pixijs/renderer/canvas').CanvasRenderer): void;
    }

    interface Container
    {
        _renderCanvas(renderer: import('pixijs/renderer/canvas').CanvasRenderer): void;
    }
}
